# Toxicity-dataset
This project focuses on building a machine learning model to classify samples as Toxic or Non-Toxic using the Class column as the target variable. The workflow follows a complete data science pipeline including Exploratory Data Analysis (EDA), data preprocessing, feature selection, and model training.
